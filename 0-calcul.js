/**
 * calculateNumber adds up two numbers
 * @param {Number} a number to add
 * @param {Number} b number to add
 * @author Moses Oyet
 */

function calculateNumber( a, b) {
	var sum = math.round(a) + math.round(b)
	return sum;
};
